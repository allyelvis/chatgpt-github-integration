'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
let removeMext = null;

exports.default = data => {
  if (process.env.RUN_ENV !== 'web') {
    // 如果发现在服务器上执行该方法，说明有问题
    console.warn('removeMext方法在服务端不生效');
  } else {
    if (typeof data === 'function') {
      removeMext = data;
    } else {
      if (typeof removeMext === 'function') {
        removeMext(data);
      }
    }
  }
};

exports.clean = () => {
  removeMext = null;
};
