export default class EventEmitter {
    listeners: {};
    on(this: any, event: string, cb: Function): void;
    emit(this: any, event: any, ...data: any[]): any;
    exist(this: any, event: any): boolean;
    off(this: any, event: any): void;
}
