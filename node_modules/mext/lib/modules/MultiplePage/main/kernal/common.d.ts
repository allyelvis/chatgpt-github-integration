import { MatchedRoute } from 'mext-types';
import Base from './base';
import { ILifeCycletoFrom } from './type';
export default class Common extends Base {
    setParam(_location: {
        pathname: string;
        search: string;
        data?: {
            scroll: boolean;
            x: number;
            y: number;
        };
    }): void;
    loadBundles(new_match_routes: Array<MatchedRoute<{}>>, _location: {
        pathname: string;
        search: string;
    }): Promise<void[]>;
    loadInitialProps(new_match_routes: Array<MatchedRoute<{}>>, _location: {
        pathname: string;
        search: string;
    }, diffRoutes: any, pathname: string): Promise<void>;
    modal(to: ILifeCycletoFrom, from: ILifeCycletoFrom, component: any): Promise<unknown>;
}
