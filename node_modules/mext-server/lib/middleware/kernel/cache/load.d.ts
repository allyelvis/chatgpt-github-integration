import { IContext } from 'mext-types';
import * as LruCache from 'lru-cache';
export declare let pageCache: LruCache<unknown, unknown>;
declare const cacheUtil: {
    init(config?: LruCache.Options<string, string> | undefined): void;
    _getPath(ctx: IContext): string | string[] | undefined;
    get(ctx: IContext): any;
    set(ctx: IContext, html: string): void;
};
export default cacheUtil;
