'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
/**
 * 由于路由是按需加载，这里保存路由按需加载后的组件
 */
// {"path":"component"}

const routes = {};

const set = (path, component) => {
  routes[path] = component;
};

const get = path => routes[path];

exports.default = {
  set,
  get
};
