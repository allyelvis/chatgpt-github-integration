'use strict';
/**
 * 首次加载的参数
 */

Object.defineProperty(exports, '__esModule', {
  value: true
});
const params = {
  ssr: false,
  firstRender: true,
  isRenderRouter: false,
  basename: '',
  routes: []
};

exports.set = ({ ssr, firstRender, basename, isRenderRouter, routes }) => {
  if (typeof firstRender !== 'undefined') {
    params.firstRender = firstRender;
  }

  if (typeof ssr !== 'undefined') {
    params.ssr = ssr;
  }

  if (typeof basename !== 'undefined') {
    params.basename = basename;
  }

  if (typeof isRenderRouter !== 'undefined') {
    params.isRenderRouter = isRenderRouter;
  }

  if (typeof routes !== 'undefined') {
    params.routes = routes;
  }
};

exports.get = () => params;

exports.default = {
  set: exports.set,
  get: exports.get
};
