import { MatchedRoute, IAny, ITextObj } from 'mext-types';
import { Request } from 'koa';
declare const _default: (match_routes: MatchedRoute<{}>[], search: string, context: {
    req?: Request | undefined;
    location?: IAny | undefined;
    query: ITextObj;
    setMext: (obj: Object) => void;
}, new_match_routes?: MatchedRoute<{}>[] | undefined, errorPath?: any) => Promise<{
    routes: MatchedRoute<{}>[];
    props: IAny;
}>;
/**
 * 主要用来初始化所有和当前路由关联的page级别组件的getInitialProps方法
 * match_routes 经过路由diff计算后的路由集合
 * search 当前访问地址的search部分
 * context 当前传值
 * new_match_routes 当前匹配到的完整数组集合
 */
export default _default;
