'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
let customLog;

const error = (msg, type = '', ...args) => {
  if (customLog && customLog.error) {
    return customLog.error(msg, type, ...args);
  }

  if (type) {
    console.error(`[${type}]:`);
  }

  console.error(msg);
};

exports.default = {
  error
};

function set(log) {
  customLog = log;
}

exports.set = set;
