'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
/* eslint-disable @typescript-eslint/no-namespace */

const mext_utils_1 = require('mext-utils');
/**
 * 自定义history API
 *
 * push、replace、go、goBack、goForward
 */

const history = {
  push(...rest) {
    const history = mext_utils_1.mextHistory.getHistory();
    history.push(...rest);
  },

  replace(...rest) {
    const history = mext_utils_1.mextHistory.getHistory();
    history.replace(...rest);
  },

  go(n) {
    const history = mext_utils_1.mextHistory.getHistory();
    history.go(n);
  },

  goBack() {
    const history = mext_utils_1.mextHistory.getHistory();
    history.goBack();
  },

  goForward() {
    const history = mext_utils_1.mextHistory.getHistory();
    history.goForward();
  }
};
exports.default = history;
